{
  "name": "api-galaxy",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-root tooling only. The web app has its own package.json in apps/web.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  },
  "comment": "The browser tests live in tests/e2e/ at the repo root, so Node resolves their imports from here rather than from apps/web/node_modules. Keeping this file to test tooling only avoids hoisting the web app's dependency tree and duplicating ~500 MB."
}
